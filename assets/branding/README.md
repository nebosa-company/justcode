# Branding assets

Raw source artwork. Nothing here is consumed by the build — the icons that ship
with the app live in `src-tauri/icons/` and `src/icons.js`. Keep originals here
so a future redesign starts from the source rather than from an exported icon.

| File | Size | Notes |
| --- | --- | --- |
| `justcode-logo-sheet.png` | 1024×559 | "jc" monogram wordmark, shown at four sizes on a light background. Not the icon currently shipped. |
