# User voice — cycle 3 pass, 2026-07-29

**READ.** The most consequential pass of any cycle so far: four decisions that
had been pending since cycle 1 were all answered.

## What the operator asked for

| Date | Quote | Reading |
|---|---|---|
| 2026-07-29 | "Lets start again the cycle to implement really all harness features" | The cycle's goal. Not a requirement — a scope instruction, and the reason this cycle plans all 88 remaining rather than the next ten. |
| 2026-07-29 | "show them one after another so I can choose" | Method, not product. Recorded because it worked: the same three conflicts asked *as a batch* went unanswered for two cycles, and asked *singly* were answered in minutes. |

## Decisions

| | Answer |
|---|---|
| `I-3` | Full panel as specified — vision clause 6 rewritten to match |
| `O-6` | Outbound only; `/btw` the sole return path |
| `G-13` | Split — churn banned, evidence versioned |
| `X-4` | `windows-sys` approved; nothing else may add a crate without another approval |
| Scope | **Make it self-running first** — batches 11 and 12 |

## What this pass changes about the loop

Perpetum C.6 says to present conflicts as a batch and continue without waiting.
That is right about *not blocking*, and this cycle is evidence it is wrong about
*batching*: three conflicts presented together were ignored twice, and the same
three presented one at a time were resolved immediately.

Not filed as a requirement — it is a fact about how the method's C.6 is best
performed, and it belongs in the journal where the next cycle's operator will
read it. Recorded here because a lesson about asking is worth as much as a
lesson about building.

**Items captured:** 0 new requirements. 4 decisions, 3 requirement texts amended.
