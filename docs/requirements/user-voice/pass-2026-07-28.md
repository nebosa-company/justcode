# User voice — pass 2026-07-28

**PARTIAL.** GitHub issues unavailable; the operator's session read in full.

## GitHub Issues — unavailable

`gh` is installed and authenticated, but the active account (`gaddlord`) cannot
resolve `nebosa-company/justcode` — the repository is private to the other
authenticated account. Switching the active `gh` account is a settings change
and is not performed unattended.

Fix, for the operator to run if they want this source live next cycle:

```bash
gh auth switch --user nebosa-company
```

Full transcript in journal record `c1/B/s07`.

## Operator session — read

The only live user-voice channel this cycle. Both items were captured as
requirements during the design pass and are cited, not re-minted (Perpetum 0.8).

| Date | Quote (abridged) | Filed as |
|---|---|---|
| 2026-07-28 | "speculate a harness ala Claude Code and OpenHands/OpenDevin that is capable to execute continuous loops and using LM Links and LM Studio as well as Deepseek APIs" | the whole of `L-*`, `M-*`, `T-*`, `V-*` |
| 2026-07-28 | "we should be able to have support for chat/response, /btw command, artifacts and full harness around managing git as well aa tools to integrate with the OS" | `C-1`–`C-12`, `A-1`–`A-7`, `G-1`–`G-14`, `X-1`–`X-12`, `M-21` |
| 2026-07-28 | "Use d:\repos\perpetum.md workflow to loop the development of the requirements" | not a product requirement — an instruction to run the loop; recorded here so the origin of cycle 1 is traceable |

## What this source is telling us

Two of the three messages expanded scope rather than reporting a defect, which
is the signature of a product with no users yet. Weight 40 is doing real work
here: with crash (100) and support (45) both silent, the operator's voice is the
highest-weighted live source in the cycle, and Phase C's top batches should
reflect what they asked for last — chat, `/btw`, artifacts, git, OS — *after*
the machinery those features stand on exists.

**Items captured:** 2 (both already filed).
