# Security scanners — pass 2026-07-28

**EMPTY, not unavailable.** The source can be read; there is nothing in it yet.

- The harness has zero source files and zero dependencies as of this pass, so
  there is no dependency tree to scan and no code for a static analyser to
  reject.
- The editor's dependency surface (`package.json`, `src-tauri/Cargo.toml`) is
  **out of scope** for this binding: JustCode is a separate product in the same
  repository, and changes under `src/` and `src-tauri/` are approval-gated
  (binding, approval boundary). Findings there would be requirements for a
  product this loop is not building.
- `cargo-audit` is not installed on this machine. Not installing it unattended;
  it is a candidate for the batch that first adds a dependency.

**Standing security requirements are already filed** from the design pass and
are not re-minted here: `S-1` (content is data, never instruction), `S-2`
(secrets by env-var reference only), `S-3` (redaction before any cloud link),
`S-4` (egress allowlist), `S-5` (never fabricate a credential), `S-6`
(`local-only` is assertable), `S-7` (nothing leaves the machine unattended).

**Becomes non-empty when:** `crates/` has code and dependencies. Perpetum E.5
runs a scan at the end of this cycle and its findings feed cycle 2's Phase B —
that is the intended path, not this pass.

**Items captured:** none.
