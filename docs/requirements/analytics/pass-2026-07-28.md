# Analytics — pass 2026-07-28

**UNAVAILABLE.**

No usage telemetry exists, in either product. Same search as the crash pass
(`c1/B/s06`) — no GA, Pendo, Amplitude, PostHog, Mixpanel or Firebase anywhere
in the tree.

Worth recording once so later cycles stop re-asking: the vision's clause 1
("never a hosted service") means there is no server to receive telemetry, and
clause 3 means the harness cannot phone home unattended. Analytics for this
product, if it ever exists, is a local file the operator reads — which is what
the journal and the progress board already are.

**Items captured:** none.
