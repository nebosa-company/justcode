# Crash analytics — pass 2026-07-28

**UNAVAILABLE.**

Weight 100, and silent. The reason matters, because at this weight a real crash
signal would outrank everything else in the cycle.

- The harness has never executed. There is no build, no release, no user, and
  therefore no crash to report.
- No crash reporter is integrated anywhere in this repository. Verified by
  search for `sentry|crashlytics|bugsnag|rollbar|telemetry` across the tree
  excluding `node_modules`: five files matched, every hit a false positive
  (`scrollbar` → `rollbar`, dictionary entries). Journal record `c1/B/s06`.

**Becomes available when:** the harness ships a release and a reporter is wired
in. That is itself a candidate requirement, but it is not filed yet — the vision
forbids a hosted service, so any reporting must be local-file-based or opt-in,
and that decision belongs in a cycle where someone is actually running the
thing.

**Items captured:** none.
