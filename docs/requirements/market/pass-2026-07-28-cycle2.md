# Market signals — cycle 2 pass, 2026-07-28

**NOT RE-READ.** Same day as cycle 1's pass.

Every signal in [cycle 1's market pass](pass-2026-07-28.md) was verified against
primary sources earlier today: DeepSeek V4 GA and the deprecation of
`deepseek-chat`/`deepseek-reasoner` on 2026-07-24, the cache-hit pricing ratio,
LM Studio's two API surfaces and `/v1/responses`, and LM Link's Tailscale-backed
remote models.

Re-running those searches hours later would produce the same answers and a
dated record implying a fresh check. That is worse than saying this plainly.

**Re-read when:** the next calendar day, or before batch 6 ships anything that
depends on a model id — whichever comes first. `M-14` exists precisely because
these facts rot: one of them rotted four days before the design was written.

**Items captured:** 0.
