# Market signals — cycle 3 pass, 2026-07-29

**RE-READ.** Due: cycle 2 skipped this source with the reason "same day", and
the day has changed. `M-14` exists because these facts rot.

| Signal | Status | Consequence |
|---|---|---|
| `deepseek-chat` / `deepseek-reasoner` | **fully retired** 2026-07-24 15:59 UTC — calls are no longer routed anywhere | Our deprecation map in `links.md` already names the replacements; nothing to change |
| `deepseek-v4-flash` | $0.14 / $0.28 per million in/out — unchanged | Price book in `links.md` still correct |
| `deepseek-v4-pro` | $0.435 / $0.87 per million — unchanged | Not configured; no action |
| Cache hits | 50–100× cheaper than misses | Confirms `M-11`'s refusal to merge hit and miss counters |

**Nothing changed in 24 hours**, which is itself the finding: the pass cost one
search and confirms the configuration is still correct. Had the model ids moved,
`M-14` would have turned it into a startup error rather than a silent failure at
the eleventh call.

**Items captured:** 0.
