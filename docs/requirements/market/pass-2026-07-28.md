# Market signals — pass 2026-07-28

**READ.** Weight 5 — lowest trust, but two of these have hard consequences for
the design, which is why they were verified against primary sources rather than
summaries.

| Signal | Evidence | Date | Consequence |
|---|---|---|---|
| DeepSeek V4 is GA: `deepseek-v4-flash`, `deepseek-v4-pro`, 1M context, 384K output, tool calls + JSON + thinking mode | [api-docs.deepseek.com pricing](https://api-docs.deepseek.com/quick_start/pricing) | 2026-07-20 | The `deepseek` link kind targets these ids. Recorded in the requirements doc §0.1. |
| `deepseek-chat` / `deepseek-reasoner` **deprecated** | same | 2026-07-24 | A design written a week earlier would already be wrong. This is the entire justification for `M-14` (no hard-coded model ids; a dead id is a startup error naming the replacement). |
| Cache-hit input priced ~50× under cache-miss ($0.0028 vs $0.14 per 1M, flash) | same | 2026-07-28 | Prompt layout is an engineering requirement, not style — `M-12`. |
| LM Studio serves both `/v1/*` and a native `/api/v0/*` with `state`, `max_context_length`, `quantization`, TTFT and tok/s | [lmstudio.ai REST v0](https://lmstudio.ai/docs/developer/rest/endpoints) | 2026-07-28 | `M-7`, `M-18`. |
| LM Studio implements `/v1/responses` | [lmstudio.ai](https://lmstudio.ai/docs/developer/openai-compat) | 2026-07-28 | Two wire protocols to support — `M-21`. |
| LM Link: remote models over a Tailscale-backed encrypted network, `lms link enable`, headless supported | [lmstudio.ai/docs/lmlink](https://lmstudio.ai/docs/lmlink) | 2026-07-28 | The `lmlink` link kind — `M-19`, `M-20`. |

## Competitor shape

Claude Code and OpenHands both optimise for a good session; neither carries loop
state across process restarts as a first-class concern, and neither treats
"prove the gate was green" as a product feature. That gap is the product thesis
(vision, "what makes it different"), so no new requirement is minted from it —
`V-1`–`V-9` already are that answer.

## Unverified, carried forward

Whether LM Link's remote models are reachable through the local `/v1` server or
only through the app and `lms` CLI. This decides the `lmlink` transport. Open
question 1 in the requirements doc; it needs a machine with a linked peer, so it
cannot be settled from a search.

**Items captured:** 0 new — all six signals map to requirements already filed.
